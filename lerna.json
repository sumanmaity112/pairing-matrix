{
  "packages": ["engine", "vue-visualiser", "frontend-app"],
  "npmClient": "yarn",
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": "main",
      "ignoreChanges": ["**/*.md"],
      "changelogPreset": "conventionalcommits",
      "message": "build: Bump new version"
    }
  }
}
