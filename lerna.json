{
  "packages": ["engine", "vue-visualiser"],
  "npmClient": "yarn",
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": "main",
      "ignoreChanges": ["**/*.md"],
      "changelogPreset": "conventionalcommits",
      "message": "build: Bump new version"
    }
  }
}
